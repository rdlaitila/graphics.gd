name: Go

on:
  push:
    branches: ["release", "staging"]
  pull_request:
    branches: ["release"]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Set up Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: "1.26"

      - name: Compute week key
        id: week
        shell: bash
        run: echo "week=$(date +%Y-%W)" >> $GITHUB_OUTPUT

      - name: Cache ~/gd directory
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: ~/gd
          key: ${{ runner.os }}-gd-weekly-${{ steps.week.outputs.week }}
          restore-keys: ${{ runner.os }}-gd-weekly-

      - name: Command Line Tooling
        run: |
          cd ./cmd/gd
          go build -o gd.exe

      - name: Test
        env:
          # Go 1.26's Green Tea GC emits an AVX-512 span-scan (scanSpanPackedAVX512) that
          # faults with SIGILL on the subset of GitHub windows-latest runners whose CPU
          # lacks AVX-512 (the gate avx512ScanPackedReqsMet doesn't check AVX512DQ, used
          # by kmovb). Disable the experiment on Windows until upstream fixes the gate.
          # See golang/go#77674.
          GOEXPERIMENT: ${{ matrix.os == 'windows-latest' && 'nogreenteagc' || '' }}
        run: |
          cd ./internal
          echo "GOEXPERIMENT=$(go env GOEXPERIMENT)"
          DEBUG_CMD=1 ../cmd/gd/gd.exe test -v
        shell: bash

  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.26"

      # Node 22+ provides the global WebSocket the headless runner uses to talk
      # to Chromium over the DevTools protocol.
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Compute week key
        id: week
        shell: bash
        run: echo "week=$(date +%Y-%W)" >> $GITHUB_OUTPUT

      - name: Cache ~/gd directory
        uses: actions/cache@v4
        with:
          path: ~/gd
          key: ${{ runner.os }}-gd-weekly-${{ steps.week.outputs.week }}
          restore-keys: ${{ runner.os }}-gd-weekly-

      - name: Command Line Tooling
        run: |
          cd ./cmd/gd
          go build -o gd.exe

      # Builds the js/wasm test binary, exports the Godot web build, serves it,
      # and runs it in headless Chromium (the runner's preinstalled
      # google-chrome), failing if the embedded `go test` fails. See
      # startup/internal/webtest/README.md.
      - name: Web Tests (headless browser)
        run: |
          GD="$PWD/cmd/gd/gd.exe" bash startup/internal/webtest/run_web_tests.sh -v

  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.26"

      - name: Compute week key
        id: week
        shell: bash
        run: echo "week=$(date +%Y-%W)" >> $GITHUB_OUTPUT

      - name: Cache ~/gd directory
        uses: actions/cache@v4
        with:
          path: ~/gd
          key: ${{ runner.os }}-gd-weekly-${{ steps.week.outputs.week }}
          restore-keys: ${{ runner.os }}-gd-weekly-

      - name: Command Line Tooling
        run: |
          cd ./cmd/gd
          go build -o gd.exe

      # The x86_64 system image only runs at usable speed with KVM. GitHub Linux
      # runners are KVM-capable but the device isn't world-accessible by default.
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      # reactivecircus/android-emulator-runner boots an x86_64 AVD (with KVM) and
      # runs `script:` once adb is connected. `gd test` exports the suite with
      # --headless (no GPU/render loop, which a CI emulator lacks), installs and
      # runs it, and fails the job on any test failure. See run_android_tests.sh
      # + Android.Test.
      - name: Android Tests (x86_64 emulator)
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          arch: x86_64
          target: google_apis
          force-avd-creation: false
          emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -no-snapshot
          disable-animations: true
          script: |
            GD="$PWD/cmd/gd/gd.exe" bash startup/internal/androidtest/run_android_tests.sh
